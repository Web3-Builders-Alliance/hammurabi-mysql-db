name: Daily Cron Job

on:
  schedule:
    - cron: '0 0 * * *'  # Schedule to run at 0 UTC daily

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9.12

      - name: Install dependencies
        run: pip install -r requirements.txt 

      - name: Run Python script
        env: 
          FLIPSIDE_API: ${{ secrets.FLIPSIDE_API }}
          MONGO_URI: ${{ secrets.MONGO_URI }}
          QUICKNODE_CLIENT: ${{ secrets.QUICKNODE_CLIENT }}
          CLOUDFLARE_ACCESS_KEY: $${{ secrets.CLOUDFLARE_ACCESS_KEY}}
          CLOUDFLARE_SECRET_KEY: $${{ secrets.CLOUDFLARE_SECRET_KEY}}
          COINGECKO_API_KEY: $${{ secrets.COINGECKO_API_KEY}}
        run: python main.py

      - name: commit files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git diff-index --quiet HEAD || (git commit -a -m "updated files" --allow-empty)

      - name: push changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
